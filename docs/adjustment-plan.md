# 白板调整清单与实施计划

以下记录第一阶段的改造与验收；当前依赖与运行方式以 [README](../README.md) 为准。
第一阶段参考 paint-board 的交互设计，保留 Vue 3 与 Fabric；不迁移其 React 代码。

## 本次实施

- [x] 画笔尺寸：1–40，默认 5，快捷档位 2/5/10/20，采用画布单位。
- [x] 画笔颜色：8 个常用色、自定义颜色、选中状态；默认红色。
- [x] 属性面板：画笔与线条、箭头、形状共用颜色和线宽，实时预览；橡皮擦固定范围，无设置面板。
- [x] 文字颜色、字号设置；单击无边框直接输入，拖拽时预览虚线范围并设置换行宽度。
- [x] 开放式箭头尖；橡皮擦拖动时显示短暂拖影，碰到对象先置灰、松开删除，可撤销。
- [x] 配置记忆：本地保存设置，校验异常数据，存储不可用时仍可绘制。
- [x] 历史记录：统一内容提交、初始快照、去重、分支截断、异步恢复锁。
- [x] 箭头注册序列化类型；多次粘贴创建独立对象；快捷键支持 macOS。
- [x] PPT 页面独立保存批注，统一翻页入口；清除批注保留背景；完成页面删除。
- [x] 同步仅响应内容变更，串行恢复最新快照，移除本地压缩往返。
- [x] 画布、事件、快捷键卸载清理；复杂实例使用 shallowRef。
- [x] 当前页 PNG 导出，包含 PPT 背景。
- [x] 构建、核心回归检查及中文使用说明。

## 验收

1. 改色、改尺寸后下一笔生效，已有内容不变，配置操作不占用撤销步骤。
2. 工具切换、刷新后恢复颜色、线宽和字号；橡皮擦保持固定范围。
3. 绘制、删除、移动、清屏可撤销；撤销后新绘制不再保留旧重做分支。
4. 箭头在撤销恢复及镜像画布中仍是箭头。
5. 文字单击无边框，拖拽可指定换行宽度；空白文本退出不留下对象。
6. 橡皮擦拖动期间对象保持存在并淡化，松开删除，撤销后恢复原不透明度。
7. 不同 PPT 页批注互不影响，返回页面能恢复；快速操作不会异步覆盖。
8. PNG 导出成功；移除不用的擦除依赖。

## 后续计划（不纳入本次）

- 文档自动保存与恢复：页面模型稳定后引入 IndexedDB，文档带 schema 版本。
- 对象属性编辑：颜色、线宽、填充、透明度、锁定与层级。
- 画布平移、适应窗口、触屏交互；先统一坐标与视口策略。
- 真正的局部擦除：当前保持命中后删除对象，另行验证 clipPath、序列化与撤销。
- 多人协作编辑、PPTX 转换、创意画笔按业务需求另行立项。
- Fabric 大版本升级须逐项回归绘制坐标、快照和交互行为。

## 实施结果

- 已完成上述本次实施项，新增 `BrushSettings.vue`、设置校验与快照历史模块。
- 移除不再需要的 `@erase2d/fabric`；开发依赖包含 Playwright，用于浏览器回归。
- Node 内置测试 3 项通过：历史分支与容量、配置校验、箭头序列化。
- 浏览器回归通过：设置生效及记忆、开放式箭头、无边框文字与拖拽换行、橡皮擦预览与撤销、重复粘贴、键盘删除、页面隔离、清屏、PNG 导出和页面删除。
- 同步改为内容提交后串行恢复最新快照；移除未使用的擦除依赖后继续降低打包体积。
- 新增 CI：锁文件安装、核心测试、构建、浏览器回归。远程执行结果需以 GitHub Actions 为准。
- 本地浏览器检查使用 Chromium 138；本环境默认 Playwright 浏览器下载不可用，使用独立浏览器可执行文件完成检查。CI 使用 Playwright 配套浏览器。

## 第二阶段：同步与结构

- [x] Fabric 7.4、Vite 8 与 Tailwind CSS v4 集成，保留 Fabric 6 的左上角绘制坐标；TypeScript 暂保留与 vue-tsc 相容的 5.9.3。
- [x] 采用 gzip / Base64 编码与解码的本地同步模拟，注释标明未来的发送和接收位置；无需 iframe 或 WebSocket 服务。
- [x] 将 Tailwind 实用类内联到 Vue 模板；全局入口整理到 `src/styles/index.css`。
- [x] 清理未使用的图像，组件私有 SVG 就近存放，共享素材放在 `src/assets`。
- [x] 完善面向用户的 README 与面向 AI agent 的 AGENTS.md。

## 第三阶段：工程规范与目录

- [x] 接入 Oxlint 和 Oxfmt，纳入 CI 与开发命令；在关键时序和边界添加中文 JSDoc。
- [x] 将环境声明放入 `src/types/`，统一使用根目录的 `AGENTS.md`。
- [x] 保持共享素材在 `src/assets/`、组件专用 SVG 就近存放；删除未使用的 Vite 默认图标。
- [ ] `src/core/index.ts` 同时处理绘图、页面、历史、文本和擦除；后续新增相关功能时按职责逐步提取，先补行为回归，再避免一次性重写。
- [ ] 多人编辑、认证、权限、持久化与增量同步另行设计，不属于单发布者预览演示。
